pipeline {
    agent any

    stages {
        stage "terrafor init"{

            steps {
                sh 'terraform init'
            }
        }
        stage  "terraform apply" {
            steps {
                sh 'terraform apply -auto-approve'
            }
        }
    }
    post {
        always {
            sh 'rm -rf *'
        }
    }
}